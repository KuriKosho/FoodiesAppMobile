import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const input = ({label, type, hint}) => {
  return (
    <View>
      <Text>input</Text>
    </View>
  )
}

export default input

const styles = StyleSheet.create({})